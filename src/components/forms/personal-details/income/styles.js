import styled from "styled-components";

export const Container = styled.div`
  label {
    margin: 1rem;
    font-weight: 500;
    cursor: pointer;
  }
  input {
    cursor: pointer;
    margin: 0.5rem 0;
  }
`;
